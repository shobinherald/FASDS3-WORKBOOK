# Change server port (optional)
server.port=8081

# Application name (optional)
spring.application.name=klu-course-crud

# Enable detailed error messages (optional for learning)
server.error.include-message=always